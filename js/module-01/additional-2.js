'use strict';

const sharm = 15;
const hurgada = 25;
const taba = 6;

let userGroup = prompt('Выберите группу: sharm - 0, hurgada - 1, taba - 2');

if (userGroup === 0 || userGroup === 1 || userGroup === 2) {

    booking = prompt('Введите число необходимых мест');

} else {

    alert("Ошибка ввода");

}